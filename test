for(let i = 1; i <= 9; i++){
    let row = '';// 准备一个空字符串，用来拼接每一行的内容

    //内容循环：控制列数，从1到当前的行数i
    for(let j = 1; j <= i; j++){
        //\t是制表符，用来让输出的等式对齐，看起来更美观

        row += `${j} × ${i} = ${i * j}\t`;
    }
    console.log(row);
}